# Evaluation-only future resolution

This file is deliberately outside `sources/` and outside the source manifest. Runtime code must not read it. A later authorized rerun at 500 cycles and 60 degC with evaluator v2.1 produced 78 percent retained flux after the flow sensor was recalibrated.
